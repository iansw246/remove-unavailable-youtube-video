import { createTheme } from "@mui/material";

const theme = createTheme({
    typography: {
        h1: {
            fontSize: "4.5rem",
        },
        h2: {
            fontSize: "3rem",
        },
        h3: {
            fontSize: "2.5rem",
        },
    },
});

export default theme;